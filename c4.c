#include<stdio.h>
main()
{
	int age;
	printf("age");
	scanf("%d",&age);
	if(age>=4&&age<=6)
	printf("kg");
	if(age>=7&&age<=9)
	printf("primary");
		if(age>=10&&age<=14)
	printf("high school");
		if(age>=15&&age<=16)
	printf("clg");
		if(age>=16&&age<=28)
	printf("graduation");
		
	
}
